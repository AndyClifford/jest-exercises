const { multiply } = require('../multiply');

test('the multiply function returns 8 when it is called with 2 and 4 as parameters', () => {
  const result = multiply(2, 4);
  expect(result).toEqual(8);
});

test('TODO', () => {
  // TODO
});

test('TODO', () => {
  // TODO
});

test('TODO', () => {
  // TODO
});
